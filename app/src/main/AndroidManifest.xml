<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="threadlifecycle.moi.android.fr.project">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDfb1tcZ7vjKP-uYcoYKOGw27l7V0d3LFg" />

        <activity
            android:name=".GameWatcher"
            android:label="@string/app_name"
            android:theme="@style/GameWatchDesign">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Localisation"
            android:label="@string/title_activity_localisation"
            android:parentActivityName=".GameWatcher"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".GameWatcher" />
        </activity>
        <activity
            android:name=".Searching"
            android:label="@string/title_activity_searching"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".GameWatcher"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".GameWatcher" />


        </activity>
    </application>

</manifest>